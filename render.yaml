services:
  - type: web
    name: myrenderproject
    env: python
    buildCommand: pip install -r requirements.txt && python init_db_script.py
    startCommand: gunicorn app:app
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: SECRET_KEY
        value: supersecretkey123change
